# ===== Build do JUCE (pesado e regenerável) =====
plugin/build/
plugin/build-mac/
build/

# ===== Binários e artefatos de compilação =====
*.exe
*.dll
*.lib
*.obj
*.pdb
*.ilk
*.exp
*.vst3
*.component
*.o
*.a

# ===== Ferramentas / relatórios =====
textidote.jar
textidote-report.*

# ===== Áudio gerado (grande e reproduzível) =====
*.wav
# exceção: o excerto usado nos exemplos do README fica versionado (Vocadito, CC-BY 4.0)
!exemplo-antes.wav

# O texto do TCC (LaTeX) vive em repositório próprio — gacherubini/TCC-TEXT.
# Por isso não há mais regras de artefato de LaTeX aqui.

# ===== Config local de ferramentas =====
.claude/
.superpowers/

# ===== Sistema =====
Thumbs.db
.DS_Store

# Referencia de saida gravada por baseline.sh (WAVs grandes; o resumo.txt basta)
baseline/*.wav

# Ambiente Python local (numpy/soundfile para os scripts de medicao em python/).
.venv/
